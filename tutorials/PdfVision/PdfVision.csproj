<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\src\PocketFlow\PocketFlow.csproj" />
        <ProjectReference Include="..\SharedUtils\SharedUtils.csproj" />
    </ItemGroup>

    <ItemGroup>
        <!-- Copy the pdfs sample directory to the build output -->
        <Content Include="pdfs\**\*" CopyToOutputDirectory="PreserveNewest" />
    </ItemGroup>

</Project>
